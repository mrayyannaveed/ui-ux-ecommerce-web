/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.pixabay.com', 'images.unsplash.com', 'plus.unsplash.com','mockuptree.com', 'unblast.com', 'i.pinimg.com',
      'i.pinimg.com', 'cdn.sanity.io', 'www.byoglogo.com'
    ],
  },
};

export default nextConfig;